import Popup from './Popup.js';

class PopupWithForm {
  constructor(popupEnviar, popupSelector) {
    this.popupEnviar = popupEnviar;
    this._popupSelector = popupSelector;
  }

  _getInputValues() {
    //recopila datos de todos los campos de entrada.
  }
  setEventListeners() {
    //Modifica el método padre
  }
}
